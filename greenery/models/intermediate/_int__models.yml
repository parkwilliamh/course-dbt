version: 2

models:
  - name: int_session_events_agg
    description: Summary information for sessions
    columns:
      - name: session_guid
        tests:
          - unique
          - not_null
  - name: int_session_events
    description: "occureence of event types in sessions"
  - name: int_product_events
    description: "occureence of event types in sessions"